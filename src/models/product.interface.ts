export interface Product {
    id: number,
    model: string,
    brand: string,
    color: string,
    category: string,
    imgUrl: string,
    inches: string,
    desc: string,
    features: string[],
    specs: string[]
}